const arr = [
    { d: '2021-11-05', t: 21.2, p: 36.9, h: 12.5 },
    { d: '2021-11-05', t: 21.2, p: 36.9, h: 12.5 },
    { d: '2021-11-05', t: 21.2, p: 36.9, h: 12.5 },
];

function convertArrObjectsToArrProperties(arrObjects) {
    const out = [];
    const tmp = {};
    if (arrObjects.length !== 0) {
        let o = arrObjects[0];
        for (const key in o) {
            tmp[key] = [];
        }

        arrObjects.forEach(el => {
            for (const key in el) {
                tmp[key].push(el[key]);
            }
        });
        // after^^^
        //     {
        //         d: ['2021-11-05', ...],
        //         t: [21.2, ...],
        //         p: [36.9 ...],
        //         h: [12.5 ...],
        //     },

        for (const k in tmp) {
            out.push({ [k]: tmp[k] });
        }
    }
    return out;
}

const res = convertArrObjectsToArrProperties(arr);
res;
//==========================================
let ob2 = { d4: '' };
let obj1 = { d1: { d2: 2 }, b1: { b2: 3 } };
obj1.d1 = { ...obj1.d1, ...ob2 };
obj1.d1 = { ...obj1.d1, d5: '' };
obj1;
//==========================================
let obj2 = {
    d: [1],
    c: [2],
}

let obj3 = { c: obj2.c };
obj3;
  //==========================================

  //==========================================
  //==========================================
  //==========================================

  //==========================================
  //==========================================
  //==========================================

  //==========================================
  //==========================================
  //==========================================

  //==========================================
  //==========================================